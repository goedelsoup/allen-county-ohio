{
  "name": "allen-county-web",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "A public reading of the Allen County corpus, built only from its exported feeds",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "lint": "oxlint",
    "test": "vitest run",
    "boundaries": "node scripts/fetch-boundaries.mjs"
  },
  "dependencies": {
    "@deck.gl/core": "^9.1.0",
    "@deck.gl/layers": "^9.1.0",
    "astro": "^5.6.0",
    "plotly.js-basic-dist-min": "^4.0.0"
  },
  "devDependencies": {
    "oxlint": "^1.0.0",
    "typescript": "^5.8.0",
    "vitest": "^3.1.0"
  }
}
