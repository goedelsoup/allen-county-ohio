---
// A chart, its caption, and the table that says the same thing without colour.
//
// The table is not a fallback. Every value on this site is reachable without reading a
// tooltip and without distinguishing two hues, which is what makes the chart an enhancement
// rather than the only way in.
interface Props {
  id: string
  title: string
  caption?: string
  /** Column headings for the table view. */
  columns: string[]
  rows: (string | number)[][]
  /** Height of the plot area including its axis band. */
  height?: number
}

const { id, title, caption, columns, rows, height = 340 } = Astro.props
---

<figure class="chart">
  <figcaption>
    <p class="fig-title">{title}</p>
    {caption && <p>{caption}</p>}
  </figcaption>

  <div class="plot" id={id} style={`min-height:${height}px`} data-chart={id}></div>

  <details>
    <summary>Table view</summary>
    <div class="scroll-x">
      <table>
        <thead>
          <tr>{columns.map((c) => <th scope="col">{c}</th>)}</tr>
        </thead>
        <tbody>
          {
            rows.map((row) => (
              <tr>
                {row.map((cell, i) =>
                  i === 0 ? <th scope="row">{cell}</th> : <td>{cell}</td>,
                )}
              </tr>
            ))
          }
        </tbody>
      </table>
    </div>
  </details>
</figure>

<style>
  .chart {
    margin: 0 0 2.5rem;
    padding: 1.5rem;
    background: var(--surface-card);
    border: 1px solid var(--rule-hairline);
    border-radius: var(--radius-md);
  }
  /*
   * A figcaption already labels its figure, so the title inside one is not a section
   * heading and is not rendered as one: a chart called "Fall in drug overdose deaths"
   * under an article's h1 was an h1→h3 skip on nineteen pages, and it was never a place
   * a reader would navigate to. The type is unchanged.
   */
  figcaption .fig-title {
    margin: 0 0 0.25rem;
    color: var(--text-strong);
    font: var(--type-section);
  }
  figcaption p {
    margin: 0;
    color: var(--text-body);
    font-size: 0.9375rem;
    max-width: var(--measure-prose);
  }
  .plot {
    margin-top: 1.25rem;
  }
  details {
    margin-top: 1rem;
    border-top: 1px solid var(--rule-hairline);
    padding-top: 0.75rem;
  }
  summary {
    cursor: pointer;
    font-size: 0.875rem;
    color: var(--text-body);
  }
  table {
    border-collapse: collapse;
    margin-top: 0.875rem;
    font-size: 0.875rem;
    font-variant-numeric: tabular-nums;
    min-width: 100%;
  }
  th,
  td {
    text-align: right;
    padding: 0.4rem 0.875rem 0.4rem 0;
    border-bottom: 1px solid var(--rule-hairline);
    white-space: nowrap;
  }
  thead th {
    color: var(--text-muted);
    font-weight: 500;
    font-size: 0.75rem;
    text-transform: uppercase;
    letter-spacing: 0.03em;
  }
  th[scope='row'] {
    text-align: left;
    font-weight: 500;
    padding-right: 1.5rem;
  }
</style>
