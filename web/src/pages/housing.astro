---
import Base from '../layouts/Base.astro'
import AssertionCard from '../components/AssertionCard.astro'
import ChartFigure from '../components/ChartFigure.astro'
import { assertion } from '../lib/feeds'
import { bar } from '../lib/chart-spec'

const ring = assertion('the-ring-was-built-while-the-city-was-full')
const tenure = assertion('lima-rents-and-the-townships-own')
const borrow = assertion('borrowing-against-a-house-in-lima')
const seven = assertion('the-denial-gap-is-seven-years-old')
const composition = assertion('the-lending-gap-is-not-composition')
const gap = assertion('the-denial-gap-that-does-not-resolve')
const outside = assertion('three-thousand-people-live-outside-households')
const empty = assertion('one-lima-housing-unit-in-nine-is-empty')
const fence = assertion('the-census-drew-the-block-on-the-fence')

// `categories` and `values` are what the bar renderer reads. This helper was written with a
// line's `x` and `series` for a phase, so all five of this page's charts drew a trace over two
// undefined arrays and rendered empty — silently, because the table under each one was right
// and nothing typechecked the spec. See the check added to the gate.
const bars = (a: typeof tenure, yTitle: string) =>
  bar(
    a.figures.map((f) => f.label),
    a.figures.map((f) => f.value),
    { yTitle },
  )

const tenureSpec = bars(tenure, 'Per cent of occupied units owner-occupied')
const borrowSpec = bars(borrow, 'Per cent of applications denied')
const gapSpec = bars(gap, 'Per cent of applications denied')
const sevenSpec = bars(seven, 'Per cent of applications denied')
const compositionSpec = bars(composition, 'Denials, 2018–2024')
const outsideSpec = bars(outside, 'People')
const emptySpec = bars(empty, 'Per cent of housing units vacant')
---

<Base
  title="Housing"
  lede="What Allen County is built of, when it was built, and who was lent money against it."
>
  <section>
    <h2>Who is not in a house at all</h2>
    <AssertionCard assertion={outside} />
    <p>
      Everything else on this page counts housing units and the people in them. This is the
      remainder: 3,522 people the 2020 census found in Allen County who were not living in a
      household, in forty-three of its 3,552 blocks. The county records no juvenile facility, no
      psychiatric-hospital population and no military quarters.
    </p>
    <ChartFigure
      id="group-quarters"
      title="Allen County's group quarters population by type, 2020"
      caption="2020 Census Redistricting Data Summary File, Table P5, summed from block records. 'Shelters and group homes' is the census's 'other noninstitutional facilities', which its Appendix B defines as emergency and transitional shelters for people experiencing homelessness, group homes intended for adults, and residential treatment centres."
      columns={['Type', 'People']}
      rows={outside.figures.map((f) => [f.label, String(f.value)])}
    />
    <script type="application/json" data-chart-spec="group-quarters" set:html={JSON.stringify(outsideSpec)} is:inline />
    <p>
      Lima holds 2,210 of the 3,522 on a third of the county's people, including every one of the
      1,513 in correctional facilities. The nursing homes are the only institution the county
      spreads out: 966 people in fifteen blocks across the city and seven townships, the largest of
      them holding 132. The 788 in student housing are mostly one campus — 494 of them at Bluffton
      University — and the 255 in shelters, group homes and treatment centres are mostly one
      downtown tract, which holds 124 of them.
    </p>
    <AssertionCard assertion={fence} />
    <p>
      For two phases this site could say that one Lima block held 1,360 people and no housing units
      and could not say what it was. The county's address file put a single point inside it, an
      auto wrecking yard, and put the three institutional addresses it carries half a mile west.
      The join that worked was between two files the Census Bureau draws from the same polygon: the
      block and the landmark carry the same land area to the square metre and the same internal
      point to seven decimal places, for Allen Correctional Institution and again for Oakwood. One
      of the three names in the county's file is a prison that closed in 2004.
    </p>
    <p>
      That also explains a puzzle on the elections side of this corpus. The precinct containing
      both institutions has the lowest voter-registration ratio of the county's eighty-eight, at
      0.28 per resident against a median of 0.68. Take the 1,513 out and it is 0.53, inside the
      band every other precinct occupies. The lowest registration rate in Allen County is a
      prison, and nothing on the face of the map says so.
    </p>
  </section>

  <section>
    <h2>And how many houses are empty</h2>
    <AssertionCard assertion={empty} />
    <ChartFigure
      id="vacancy"
      title="Vacancy rate, 2020, city against county against the emptiest township"
      caption="2020 Census Redistricting Data Summary File, Table H1, summed from block records. Vacancy is total units less occupied units; the file does not say why a unit is empty, which is in the Demographic and Housing Characteristics file and not in this one."
      columns={['Subdivision', 'Per cent vacant']}
      rows={empty.figures.map((f) => [f.label, f.value.toFixed(2)])}
    />
    <script type="application/json" data-chart-spec="vacancy" set:html={JSON.stringify(emptySpec)} is:inline />
    <p>
      With the group quarters count beside it the county's household size falls out exactly: 3,522
      people outside households, so 98,684 in them, in 40,935 occupied units — 2.411 people per
      household. Lima's is 2.344, the smallest of the thirteen subdivisions; Amanda Township's is
      2.691, the largest.
    </p>
    <p>
      This corpus had been using persons per housing unit as a stand-in and saying in the node that
      it was not household size, because vacant units sat in the denominator and institutional
      residents in the numerator. The open block that said so named the right table and the wrong
      obstacle: the Census API will not serve it without a key, and the flat file the API is built
      over needs no key at all. The figure was one download away for two phases.
    </p>
  </section>

  <section>
    <h2>A county sorted by its houses</h2>
    <p>
      For ten phases this corpus counted the people of Allen County and held nothing about the
      buildings they live in. It could say that a third of the county's people, two thirds of its
      poor and three quarters of its Black residents are inside Lima, and it could not say what
      Lima is made of. Six tables of the American Community Survey close that: 44,697 housing
      units, and the county's thirteen civil subdivisions add up to the county's own row fifteen
      separate times — units, occupancy, vacancy, tenure and every one of eleven year-built bands.
    </p>
    <AssertionCard assertion={tenure} />
    <ChartFigure
      id="tenure"
      title="Owner-occupancy, 2023"
      caption="Share of occupied housing units that are owner-occupied. American Community Survey five-year estimates. The county's figure is unremarkable and neither of the two things it is made of is."
      columns={['Geography', 'Per cent owner-occupied']}
      rows={tenure.figures.map((f) => [f.label, f.value.toFixed(1)])}
    />
    <script type="application/json" data-chart-spec="tenure" set:html={JSON.stringify(tenureSpec)} is:inline />
    <p>
      Lima holds 35.4 per cent of the county's housing and 48.2 per cent of its empty housing. A
      house in Shawnee Township, across the city's southern line, has a median value of $210,200
      against $95,900 in Lima — 2.2 times, on a combined margin of error of $17,965 for a gap of
      $114,300. Two townships have no published median rent at all: the survey returns its
      unavailable-value code, which is not a low figure.
    </p>
  </section>

  <section>
    <h2>The ring was built while the city was full</h2>
    <p>
      The sorting this site has described since the population page was written had no date under
      it. It has one now, from below. The median house standing in Lima was built in 1953; in Bath
      Township it is 1975, and in Shawnee and American Township 1973. Lima is 34.9 per cent
      pre-1940 where those three are between 6.4 and 7.1 per cent.
    </p>
    <AssertionCard assertion={ring} />
    <p>
      A sorting into an old cheap city and a new dear ring cannot be older than the ring, and the
      ring went up in the decade Lima reached its peak of 53,734 people and still held 48.4 per
      cent of the county. That bounds when the sorting could have happened. It does not say when
      anyone moved, and nothing here says why the building went where it did — a construction date
      is not a zoning map, a subdivision approval or a mortgage, and this corpus holds none of the
      three.
    </p>
    <p>
      One more thing the bands say. A quarter of the housing standing in Allen County was built in
      1980 or later — 11,327 units — over a span in which the county fell from 112,241 people to
      100,866. The two numbers are not a ratio and nothing here divides them. A county losing
      people built a quarter of its housing while doing it.
    </p>
  </section>

  <section>
    <h2>Buying, and borrowing against what you own</h2>
    <p>
      Every mortgage application acted on in this county is on public record, and seven years of it
      are read here: 24,269 decisions between 2018 and 2024. Lending ran to $513.1 million in 2020
      and $270.3 million in 2023, on between 175 and 209 institutions a year. One lender makes about
      a third of it — Superior Credit Union, of 540 W. Market Street in Lima, with 646 originations
      in 2023 where Rocket Mortgage made 63.
    </p>
    <p>
      This section was expected to be about buying a house in Lima, and the difference is not
      mostly there. An earlier version of this page said flatly that buying a house in Lima is no
      harder than buying one anywhere in the county. That was read off 2023 alone, which is the one
      year of seven in which Lima's purchase denial rate is the lower one; over the seven it is
      13.3 per cent against 10.8. The claim is withdrawn and the figures below replace it. What
      holds is the size of the two gaps: two and a half points on a purchase, sixteen on everything
      else.
    </p>
    <AssertionCard assertion={borrow} />
    <ChartFigure
      id="borrow"
      title="Denial rates by purpose, 2018–2024"
      caption="2018–2024, applications acted on — originated, approved and declined, or denied. 'In Lima' is the sixteen of the county's thirty-five census tracts whose population is majority Lima; they hold 32,575 of the city's 35,579 people and 3,093 who live outside it."
      columns={['Applications', 'Per cent denied']}
      rows={borrow.figures.map((f) => [f.label, f.value.toFixed(1)])}
    />
    <script type="application/json" data-chart-spec="borrow" set:html={JSON.stringify(borrowSpec)} is:inline />
    <p>
      The register's own reason moves with the geography. Twenty-one per cent of denials in Lima's
      tracts cite collateral, against 13 per cent elsewhere — 43 of 201 against 53 of 404 — where
      credit history leads in both. Collateral is the house, and the median house in Lima is worth
      $95,900 and was built in 1953. The median purchase loan originated in a Lima tract was
      $115,000; elsewhere in the county it was $175,000.
    </p>
  </section>

  <section>
    <h2>The difference that does not resolve</h2>
    <p>
      This one was published from a single year too, and seven years make it larger rather than
      smaller. The gap appears in all seven, between 5.9 and 17.0 percentage points, and never once
      reverses.
    </p>
    <AssertionCard assertion={seven} />
    <ChartFigure
      id="seven"
      title="Denial rate by applicant race, 2018–2024 pooled"
      caption="24,269 decisions across seven years. 1,334 applications record a Black applicant and 19,673 a white one; 18 per cent of all rows record no race and are in neither bar."
      columns={['Applicants', 'Per cent denied']}
      rows={seven.figures.map((f) => [f.label, f.value.toFixed(1)])}
    />
    <script type="application/json" data-chart-spec="seven" set:html={JSON.stringify(sevenSpec)} is:inline />
    <p>
      The obvious explanation is composition — that Black applicants in this county apply for
      different loans, at different incomes, with different debt and different equity. They do, and
      it is not most of the difference. Sorting every decision into a cell of loan purpose by income
      band by debt-to-income band by loan-to-value band gives 311 cells in which applications from
      both groups sit together.
    </p>
    <AssertionCard assertion={composition} />
    <ChartFigure
      id="composition"
      title="Denials among 955 applications from Black applicants, 2018–2024"
      caption="Expected denials are the sum of the white denial rate in each application's own cell of purpose, income, debt-to-income and loan-to-value. The same arithmetic applied to white applications returns exactly the number observed, which is the check that it is arithmetic."
      columns={['', 'Denials']}
      rows={composition.figures.map((f) => [f.label, f.value.toFixed(1)])}
    />
    <script type="application/json" data-chart-spec="composition" set:html={JSON.stringify(compositionSpec)} is:inline />
    <p>
      That is a ratio of 1.68 and nine standard deviations from the null. Inside those cells the
      unadjusted rates are 23.4 per cent against 12.2; reweighting the white rate to the Black
      applicants' own mix moves it to 13.9, which closes fifteen per cent of the gap.
    </p>
    <AssertionCard assertion={gap} />
    <ChartFigure
      id="gap"
      title="Denial rates by applicant race, within income bands, 2023"
      caption="Applications acted on, by the race the register derives from the applicant's own file. 598 of the county's 3,294 rows record no race at all and are in none of these bars."
      columns={['Applicants', 'Per cent denied']}
      rows={gap.figures.map((f) => [f.label, f.value.toFixed(1)])}
    />
    <script type="application/json" data-chart-spec="gap" set:html={JSON.stringify(gapSpec)} is:inline />
    <p>
      Black applicants earning between $50,000 and $99,000 were denied more often than white
      applicants earning under $50,000. That is what the file says, and it is not a finding about
      how anyone was treated: the register carries no credit score, no assets, no reserves and no
      appraisal, and credit history is the leading denial reason in every group here. This site
      publishes the outcome and the missing variables together, and the corpus has an open question
      that says what would settle it — the file's own unread columns first, then six more years of
      it, and after that a record no member of the public can see.
    </p>
  </section>
</Base>
