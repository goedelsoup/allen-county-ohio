---
import Base from '../layouts/Base.astro'
import Gloss from '../components/Gloss.astro'
import ChartFigure from '../components/ChartFigure.astro'
import StatTile from '../components/StatTile.astro'
import AssertionCard from '../components/AssertionCard.astro'
import DataTable from '../components/DataTable.astro'
import { assertion, edges, nodes, seriesById } from '../lib/feeds'
import { bar, line } from '../lib/chart-spec'

const nine = assertion('nine-offices-eleven-officers')

const money = assertion('a-grand-total-is-not-a-budget')

const moneySpec = bar(
  money.figures.map((f) => f.label),
  money.figures.map((f) => f.value),
  { yTitle: 'General Fund spending, 2025 (US$)', labelValues: true },
)

// Typed out because these are fund totals from a PDF, not properties the graph carries.
const funds = [
  ['The whole revenue report, 437 funds', '$374,841,547', '100%'],
  ['52 custodial funds numbered 9xxx', '$181,069,850', '48.3%'],
  ['— of which real estate tax collected for others', '$139,249,231', '37.1%'],
  ['The General Fund, which the county runs on', '$39,567,583', '10.6%'],
]

// Built from the graph rather than typed out, so the page cannot drift from the corpus.
// An office with no tenure would show as a blank holder rather than silently vanish.
const byId = new Map(nodes.map((n) => [n.id, n]))

const offices = nodes
  .filter((n) => n.class === 'office' && n.properties.office_type === 'elected')
  .filter((n) => n.id.includes('allen-county'))
  .toSorted((a, b) => a.label.localeCompare(b.label))

const holdersOf = (officeId: string) =>
  edges
    .filter((e) => e.relationship === 'of-office' && e.to === officeId)
    .map((e) => byId.get(e.from))
    .filter((t): t is NonNullable<typeof t> => Boolean(t))
    .map((tenure) => {
      const held = edges.find((e) => e.relationship === 'held-by' && e.from === tenure.id)
      return {
        person: (held && byId.get(held.to)?.label) ?? '—',
        began: tenure.properties.began ?? '',
        ended: tenure.properties.ended ?? '',
        label: tenure.label,
      }
    })
    .toSorted((a, b) => b.began.localeCompare(a.began))

// A current holder is one whose tenure has no `ended` — not the one with the latest start.
// Sorting by start date put the wrong sheriff here: Everett and Treglia both began in 2017,
// because Everett's term ended in the year Treglia's began.
const currentOf = (officeId: string) =>
  holdersOf(officeId).filter((h) => !h.ended)

const rows = offices.flatMap((o) => {
  const held = currentOf(o.id).toSorted((a, b) => a.began.localeCompare(b.began))
  return held.map((h, i) => [
    i === 0 ? o.label : '',
    h.person,
    h.began,
    i === 0 ? (o.properties.term_length ?? '—') : '',
  ])
})

const courts = nodes.filter((n) => n.properties.jurisdiction_type === 'court district')

const judicial = nodes
  .filter((n) => n.class === 'office' && n.properties.office_type === 'judicial')
  .toSorted((a, b) => a.label.localeCompare(b.label))

const judgeRows = judicial.flatMap((o) =>
  currentOf(o.id)
    .toSorted((a, b) => a.began.localeCompare(b.began))
    .map((h, i) => [i === 0 ? o.label : '', h.person, h.began]),
)

// Representation: the offices whose constituency is a district rather than the county. They are
// the ones that point at a division, or at the appellate court that is not the county's own.
const REP = [
  'office/member-of-the-ohio-house-for-district-78.yml',
  'office/member-of-the-ohio-senate-for-district-12.yml',
  'office/representative-in-congress-for-ohios-4th-district.yml',
]

const repRows = REP.flatMap((id) => {
  const o = byId.get(id)
  if (!o) throw new Error(`no office ${id} in the feed`)
  return currentOf(id).map((h) => [o.label, h.person, h.began])
})

const tenureCounts = offices.map((o) => [o.label, holdersOf(o.id).length])

// An office is "traced" when the corpus holds more tenures than the office has seats — that
// is, at least one predecessor. Three commissioners in three seats is not a line of holders.
const traced = offices.filter(
  (o) => holdersOf(o.id).length > Number(o.properties.seats ?? '1'),
).length

const swing = assertion('democratic-ground-moved-fastest')

const eleven = assertion('eleven-precincts')

const rising = assertion('republican-share-rising')

const halfRun = assertion('half-the-run-and-the-hole-is-one-block')
const shareSpec = line(rising.figures.map((f) => f.label), [
    { name: 'Republican share of the top-of-ticket vote', y: rising.figures.map((f) => f.value) },
  ], {
  yTitle: 'Per cent',
  tickSuffix: '%',
  labelLast: true,
})

const turnout = seriesById('jurisdiction-allen-county-government::ballots-cast,-general-election')
const presidential = seriesById('place-allen-county::presidential-votes-cast')
const governor = seriesById('place-allen-county::votes-cast-for-governor')

const votes = [...presidential.points, ...governor.points].toSorted((a, b) =>
  a.as_of.localeCompare(b.as_of),
)

const correlation = swing.figures[0]
---

<Base
  title="Government"
  lede="Nine elected offices govern Allen County. This page is about what they are, who chooses them, what they cost, and how far back this corpus can see."
>
  <div class="reading">
  <section>
    <h2>Nine offices, eleven officers</h2>
    <p>
      Ohio counties are not home-rule corporations. They are administrative subdivisions of the
      state, and the state fixes what offices they have: a board of three commissioners elected
      countywide, and eight other officers elected separately. Allen County has all nine and no
      more, and nothing on this page distinguishes it from the other eighty-seven Ohio counties
      except the names.
    </p>
    <AssertionCard assertion={nine} />

    <DataTable
      title="The elected offices of Allen County"
      caption="Holders and term commencement dates from the Ohio boards of elections' county lookup, read 30 August 2026. A term start is not a start of service: six of these officers may have held office longer, and this corpus does not know."
      columns={['Office', 'Holder', 'Term began', 'Term']}
      rows={rows}
    />

    <h3>Three seats, three days apart</h3>
    <p>
      The board's three commissioners hold terms that begin on the first, second and third day of
      January. That looks like a clerical artifact and is not one: Ohio law assigns those three
      dates precisely so the seats stay distinguishable and the board never turns over at once.
      One seat is elected two years out of step with the other two.
    </p>
    <p>
      The auditor is the only county officer whose term does not begin in January. It begins on the
      second Monday in March — 13 March 2023 for the present term, which was that Monday. Both
      rules were read out of the statute <em>after</em> the dates had been read off the elections
      roster, and each predicted what was already in hand.
    </p>

    <h3>Two offices you must be qualified to hold</h3>
    <p>
      Most of these offices ask only that the holder be an elector of the county. Two ask more. The
      coroner must be a physician licensed to practise medicine or osteopathic medicine and in good
      standing, and must have held that licence for two years before election — which in a county of
      about a hundred thousand people makes the candidate pool a profession rather than an
      electorate. The engineer must be <em>both</em> a registered professional engineer and a
      registered surveyor.
    </p>
    <p>
      The engineer's statute also disqualifies four of the county's other elected officers by name:
      the clerk of courts, the sheriff, the treasurer and the recorder may not hold it. Every one of
      those four is a node in this corpus, which is what makes that clause a constraint on the graph
      and not just a sentence in a code book.
    </p>
  </section>

  <section>
    <h2>The courts</h2>
    <p>
      {courts.length} courts sit over this ground and none of them is part of the county government. The
      Court of Common Pleas is the county's trial court, divided five ways by its own rules and
      staffed by four elected judges. The Third District Court of Appeals hears appeals from
      seventeen Ohio counties and keeps its office at 204 N. Main Street in Lima, so a court
      serving a sixth of the state is seated in this county and its records are kept by this
      county's clerk.
    </p>
    <p>
      And the Lima Municipal Court's territory is not Lima. Ohio's statute gives the general rule
      that a municipal court has jurisdiction "within the corporate limits of their respective
      municipal corporations", then names this one as an exception: <em>"The Lima municipal court
      has jurisdiction within Allen county."</em> A traffic case from Spencerville is heard by a
      court named after a city thirteen miles away.
    </p>

    <DataTable
      title="Judges elected by Allen County's voters"
      caption="Six judges across two courts. Every judicial office in this corpus runs six years and every elected administrative one runs four — computed from one county's roster, so a pattern in the data rather than a rule read anywhere."
      columns={['Court and division', 'Judge', 'Term began']}
      rows={judgeRows}
    />

    <p>
      The Third District's four judges sit on that table too. They are elected by the voters of all seventeen counties, which an earlier rule treated as putting them outside this county's record; the same rule then excluded Allen County's own state senator and state representative, elected by multi-county districts, so it went. A person is held here wherever the constituency is smaller than the state and includes ground in Allen County.
    </p>
  </section>

  <section>
    <h2>Who represents the county</h2>
    <p>
      One member of the Ohio House, one of the Ohio Senate, one of Congress. The county is split
      between districts in neither state chamber — the roster names one of each, and a split county
      would name two.
    </p>

    <DataTable
      title="Allen County's representation"
      caption="From the Ohio boards of elections' county lookup, read 30 August 2026. The House seat runs two years, the Senate seat four, and the congressional seat two — the shortest terms in this corpus, against four years for a county officer and six for a judge."
      columns={['Seat', 'Holder', 'Term began']}
      rows={repRows}
    />

    <p>
      The state legislative districts are current: Ohio's legislative map was adopted on 29 September
      2023 and is the map still in force, the one the November 2024 election ran on. The county was
      House District 4 under the previous map — a district coterminous with the county, to the byte
      — and is the 78th now.
    </p>
    <p>
      The congressional seat is a different matter, and it is the reason this page will not tell you
      which congressional district Allen County is in. Ohio adopted a new congressional map on
      31 October 2025. The county's sitting representative was elected in November 2024, under the
      map that one replaced. So the roster establishes who represents this county and not which
      district the county is now in, and the corpus has an open question that says so rather than
      closing the gap with a guess. One roster, one election, two vintages.
    </p>
  </section>

  <section class="tiles">
    <StatTile
      value={`${rising.figures[rising.figures.length - 1].value}%`}
      label="Republican share, 2020"
      note="Up 2.5 points across two presidential elections."
      tier={rising.tier}
    />
    <StatTile value="11 of 88" label="Precincts Biden won" note="Ten of them in Lima." tier={eleven.tier} />
    <StatTile
      value={correlation.literal}
      label="2016 share against 2016–2020 swing"
      note="The more Democratic a precinct was, the harder it moved right."
      tier={swing.tier}
    />
    <StatTile
      value={Number(turnout.points[0].value).toLocaleString('en-US')}
      label="Ballots cast, 2020"
      note="360 more than the presidential votes counted — an undervote of 0.7 per cent."
      tier={turnout.points[0].tier}
    />
  </section>

  <section>
    <h2>A county ninety points wide</h2>
    <p>
      The offices above are filled by an electorate that has moved one way through three
      consecutive elections, and that moved fastest where it had been most Democratic.
    </p>
    <AssertionCard assertion={rising} open />
    <ChartFigure
      id="republican-share"
      title="Republican share of the top-of-ticket vote"
      caption="Two presidential elections with a governor's race between them. The 2018 figure is a different office and a different electorate; it is on the same line because the corpus put it there, in the passage above."
      columns={['Election', 'Republican share']}
      rows={rising.figures.map((f) => [f.label, `${f.value}%`])}
      height={300}
    />
    <script type="application/json" data-chart-spec="republican-share" set:html={JSON.stringify(shareSpec)} is:inline />
    <DataTable
      title="Votes cast at the top of the ticket"
      caption="A count of ballots, not a share — and not a chart, because a presidential and a gubernatorial electorate are not the same size and plotting them on one line would say they were."
      columns={['Date', 'Contest', 'Votes']}
      rows={votes.map((p) => [
        p.as_of,
        p.node.includes('governor') ? 'Governor' : 'President',
        p.value.toLocaleString('en-US'),
      ])}
    />
    <AssertionCard assertion={eleven} />
    <AssertionCard assertion={swing} />
    <p class="note">
      The precinct-level returns those two assertions rest on are not in this corpus as data —
      by a recorded decision, the county's 88 precincts are held as one measure rather than 88
      nodes, because every one of them has the same relationship to everything else. So this
      page can tell you the shape of the result and cannot draw you a precinct map of it. The
      <a href="/map">map</a> shows the precincts as ground, with the population each holds.
    </p>
  </section>

  <section>
    <h2>What the county costs</h2>
    <AssertionCard assertion={money} />
    <p>
      The county auditor publishes the county's books — monthly revenue and expenditure for every
      fund back to 2016, and the State Auditor's audited statements back to 2010. Three better-
      known routes to the same numbers are closed to an automated client while the office that
      keeps the books is not.
    </p>
    <Gloss>
      Every elected officer of Allen County could be named here to the day before any of them could
      be costed.
    </Gloss>
    <ChartFigure
      id="office-cost"
      title="What each elected Allen County office spent from the General Fund, 2025"
      caption="The county engineer is missing from this chart because it spends nothing from this fund: it draws $8,456,595 from the Motor Vehicle and Gas Tax fund instead, second only to the sheriff. An office may draw on funds outside the General Fund, so these are not totals."
      columns={['Office', 'General Fund spending, 2025']}
      rows={money.figures.map((f) => [f.label, f.value])}
    />
    <script type="application/json" data-chart-spec="office-cost" set:html={JSON.stringify(moneySpec)} is:inline />
    <p>
      The sheriff's office costs half again as much as the county's other seven elected offices
      together — $10,786,007 against $7,113,672, of which $4,373,597 is jail operations. At the
      other end, Allen County's share of the Third District Court of Appeals is $30,542, the
      smallest departmental line in the fund and what one of seventeen counties pays toward a bench
      they choose together.
    </p>
    <p>
      The fund those offices spend from took in $39,567,583 in 2025, and more than half of it is
      sales tax. Interest on the county's cash came to $5,148,389, which is more than the property
      tax levied for this fund — so about an eighth of the county's general revenue arrives without
      anyone having voted for it.
    </p>
    <h3>The number to be careful with</h3>
    <p>
      The same report's grand total is $374,841,547, and the county does not have it.
    </p>
    <DataTable
      caption="Nearly half the county's books is money it holds for somebody else. Fifteen funds took in and paid out the same amount to the cent — second-half real estate at $58,725,512.16, federal withholding at $4,113,093.70 — and a fund whose two sides match exactly is a conduit, not an income."
      columns={['What', 'Revenue, 2025', 'Share']}
      rows={funds}
    />
    <p>
      The $139 million is the real estate tax the treasurer collects for twelve school districts,
      two cities, twelve villages, twelve townships, a library and a park district. It is the
      largest number in this county's public accounts and the county's own share of it is 3.3 per
      cent. The audited books tell the same kind of story from the other side: Allen County's
      liabilities rose $36,758,891 between 2022 and 2023 and it borrowed nothing — the movement is a
      pension line revalued under an accounting standard. What it owes to lenders is $16,281,525.
    </p>
    <p>
      Where that $139 million comes from is the other half of the same account. Allen County's
      taxable property is worth $2,451,331,690 in the newest audited report, so one mill raises
      $2,451,332 and the county's own 11.40 mills raise about $28 million of it. Thirty-six
      taxing districts levy on that base, six authorities to each, and school millage is between
      half and three quarters of the full rate in every one of them.
      <a href="/read/what-a-mill-is-worth">What a mill is worth</a> follows the rate down to the
      district and finds one that stopped levying.
    </p>
  </section>

  <section>
    <h2>What is still missing</h2>
    <p>
      Every holder of these offices before the current term, except the sheriff's. The powers each
      office actually exercises, as against the statute that creates it. The subject-matter
      jurisdiction of both trial courts — the corpus has established their territories and neither's
      competence. And the appointed officials and boards that do much of a county's work without
      appearing on any ballot, which is now the largest block of officeholders missing from this record, the townships having been named.
    </p>
    <p>
      Delphos is the one government on this page that is knowably incomplete. Its ward council seats
      are voted on inside their wards, and 3,182 of the city's people live in Van Wert County, so an
      Allen County roster cannot see any ward west of the line. Ten of its seats appear here and
      they are not the whole of its council.
    </p>

    <h3>How far back this page can see</h3>
    <p>
      Not far, and unevenly. Of the nine offices, {traced} holds any predecessor at all — the
      sheriff, whose own office publishes a roster back to 1831. The other eight have the present
      term and nothing before it, because the source that names them is a roster of who holds
      office now.
    </p>
    <p>
      That asymmetry is the reason this page exists at all: one office in extraordinary detail and
      eight missing entirely, which reads as a worked class and is not one.
    </p>

    <h3>The presidential record is exactly half read, and the unread half is one block</h3>
    <AssertionCard assertion={halfRun} />
    <p>
      That shape is the useful part. A record with holes scattered through it is a record you keep
      chipping at; this one has a single wall in it, and the wall is the nineteen elections between
      the county that voted for Bryan and the county that voted for Trump. Whatever turned Allen
      County Republican happened behind it. Ohio's own election office would answer the question and
      its website refuses every client that asks.
    </p>
    <Gloss>
      For nine phases the office class held two members — the sheriff, with thirty-nine tenures back
      to 1831, and the mayor of Lima — and the depth of the first made the class look finished.
    </Gloss>

    <DataTable
      title="Tenures held per office"
      caption="What the corpus has read, not what the county has had. A count of one means the present term and no predecessor."
      columns={['Office', 'Tenures in the corpus']}
      rows={tenureCounts}
    />
  </section>

    <section class="onward">
      <h2>Where this goes on</h2>
      <ul>
        <li>
          <a href="/read/what-a-mill-is-worth">What a mill is worth</a>
          <span class="why">One mill raises $2,451,332, no two of thirty-six districts charge the same, and one of them stopped being a village.</span>
        </li>
        <li>
          <a href="/read/the-democratic-column">The Democratic column</a>
          <span class="why">Five elections added to the modern run, and a third of one column gone in eight years while the other grew.</span>
        </li>
        <li>
          <a href="/read/the-government-below-the-county">The government below the county</a>
          <span class="why">Thirteen townships, nine villages and a city — and appointment is how two of them get filled.</span>
        </li>
        <li>
          <a href="/read/the-county-changed-sides">The county changed sides</a>
          <span class="why">Democratic ground for thirty years, and it did not turn with 1896.</span>
        </li>
        <li>
          <a href="/read/the-mayors-and-where-the-office-went">The mayors, and where the office went</a>
          <span class="why">Three books, four slots, and a year with nobody in it.</span>
        </li>
        <li>
          <a href="/read/three-rosters-one-checkable">Three rosters, one checkable</a>
          <span class="why">A 1906 book prints a line of holders for every office; only one roster can be tested.</span>
        </li>
        <li>
          <a href="/read/who-polices-the-county">Who polices the county</a>
          <span class="why">A step in the reported numbers that is not a crime wave.</span>
        </li>
        <li>
          <a href="/read/three-cents-in-the-dollar">Three cents in the dollar</a>
          <span class="why">The county's public library raises three per cent of its money locally.</span>
        </li>
        <li>
          <a href="/read#government">The reading room</a>
          <span class="why">Every shorter piece filed under this section.</span>
        </li>
      </ul>
    </section>
  </div>

  <script>
    import { renderCharts } from '../scripts/charts'
    renderCharts()
  </script>
</Base>
